import { ThemeOptions } from '@material-ui/core';

export const themeObject: ThemeOptions = {
  palette: {
    type: 'light',
  },
  overrides: {
    MuiPaper: {
      root: {
        // padding: '24px',
      },
    },
  },
};
